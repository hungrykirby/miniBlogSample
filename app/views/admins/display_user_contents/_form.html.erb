<%= form_with(model: [:admins, :display_user, display_user_content], local: true) do |form| %>
  <% if display_user_content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(display_user_content.errors.count, "error") %> prohibited this one from being saved:</h2>

      <ul>
        <% display_user_content.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>