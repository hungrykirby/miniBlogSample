<%= form_with(model: display_user, local: true) do |form| %>
  <% if display_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(display_user.errors.count, "error") %> prohibited this display user from being saved:</h2>

      <ul>
        <% display_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :kana %>
    <%= form.text_field :kana %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>